include 'cm-web', 'cm-repository', 'cm-model', 'cm-jcrrepository'//, 'spring-modules-jcr'

rootProject.name = 'ContentManager'

rootProject.children.each {
    //iterate through all subprojects accessible through root project
    it.buildFileName = it.name + '.gradle' - 'cm-'
}
